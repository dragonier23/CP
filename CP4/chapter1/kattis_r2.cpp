#include <iostream>
#include <fstream>
#include <string>
#include <regex>
#include <cmath>
#include <vector>

using namespace std;

int main() {
    int r1, s; 
    scanf("%d %d", &r1, &s);
    printf("%d\n", s*2 - r1);
    return 0;
}